Instructions

1.- Open "tools"
2.- Execute "compile_vs2019.bat"
3.- A VS2019 solution should have been created in the "build" directory "build". This is a global solution that includes every project (in a separate project file) that we will work with through the course
4.- Double click on the solution and open the VS IDE
5.- Select the project you want to work in


Proyecto en el que se crean y utilizan distintos tipos de datos abstractos (vector, lista, doble lista, stack, pila),
de manera eficiente y óptima. La gestión de los tipos de datos incluye reservar memoria, resize, reset, destroy...
Incluye tests para su comprobación.